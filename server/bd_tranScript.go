package server

var appid, appkey = "00000000000000", "XXXXXXXX"

func translator(transcript, from, to string) (string, error) {
	// bt := utils.CreateBaiduTranslator(appid, appkey)
	// chinsesTranScript, err := bt.Translate(transcript, from, to)
	// if err != nil {
	// 	zap.Log.Errorf("Search TranScript CreateBaiduTranslator err err", err.Error())
	// 	return "", err
	// }
	// return chinsesTranScript, nil
	return "", nil
}
